<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <!-- LeakCanary -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute"
        >

        <!-- Flipper initialization in debug builds only -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            >
            <meta-data
                android:name="io.github.wykopmobilny.initializers.FlipperInitializer"
                android:value="androidx.startup"
                />
            <meta-data
                android:name="io.github.wykopmobilny.initializers.DebugToolsInitializer"
                android:value="androidx.startup"
                />
        </provider>

        <!-- Debug state receiver - query app state and control app via ADB -->
        <receiver
            android:name="io.github.wykopmobilny.debug.DebugStateReceiver"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="io.github.wykopmobilny.debug.DEBUG_STATE" />
                <action android:name="io.github.wykopmobilny.debug.DEBUG_CLEAR_CACHE" />
                <action android:name="io.github.wykopmobilny.debug.DEBUG_LOGOUT" />
                <action android:name="io.github.wykopmobilny.debug.DEBUG_SWITCH_TAB" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
