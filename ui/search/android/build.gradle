plugins {
    id("com.starter.library.android")
}

android {
    buildFeatures {
        viewBinding = true
    }
}

dependencies {
    api(projects.ui.search.api)
    implementation(projects.ui.base.android)
}
