plugins {
    id("com.starter.library.android")
}

dependencies {
    implementation(projects.ui.base.android)
    api(projects.ui.promoted.api)
    implementation("androidx.paging:paging-runtime:3.0.1")
}
